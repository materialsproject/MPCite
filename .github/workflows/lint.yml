name: linting

on:
  push:
    branches: [master, linting-workflow]
  pull_request:
    branches: [master, linting-workflow]
  workflow_dispatch:

# TODO: setup linting with uv/ruff
# informed by testing.yml and https://medium.com/@sidharthvijayakumar7/automating-pylint-in-github-workflow-80c84b2ff243 and ruff documentation
jobs:
  linting:
    strategy:
      matrix:
        os: ["ubuntu-latest"]
        python-version: ["3.11", "3.12", "3.13"]
      fail-fast: false
    name: mp-cite (${{ matrix.os }}/py${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Install uv and set up the python version
        uses: astral-sh/setup-uv@v6
        with:
          python-version: ${{ matrix.python-version }}
          version: "latest"

      - name: Create virtual environment # this will allow us to install the dependencies in an isolated environment, for each python version too, which is already specified when uv is setup.

      - name: Install ruff
        run: |
          uv pip install ruff
      # Update output format to enable automatic inline annotations
      - name: Analyzing the code with ruff
        run: ruff check --output-format=github
