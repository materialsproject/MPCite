name: linting

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv and set up the python version
        uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.12"
          version: "latest"

<<<<<<< HEAD
=======
      - name: Install ruff
        run: |
          uv pip install ruff
      # Update output format to enable automatic inline annotations
>>>>>>> 27ac04f (Using uv to install ruff dependency, using uv-cache and removing pip installation)
      - name: Analyzing the code with ruff
        run: uvx ruff check --output-format=github
